app.factory("errors", function($rootScope){
  errors = {}

  function createCatcher(path) {
    parts = path.split('.');

    return function(error) {
      
    }
  }

  return {
      catch:
  };
});
